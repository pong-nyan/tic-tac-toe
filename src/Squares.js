export default function Squares() {
	return (
		<div>
		<button className="square" onClick={() => alert("Click!")}> x </button>
		<button className="square" onClick={() => alert("Click!")}> x </button>
		<button className="square" onClick={() => alert("Click!")}> x </button>
		</div>
	);
}
